package assessment;

import java.util.ArrayList;
import java.util.List;


public class MoreVowels {

    /** You are provided a listOfStrings iterate through the List and remove all Strings that have EXACTLY ONE vowel.
     * Vowels for this exercise are (A, E, I, O, U, Y)
     *
     * @param listOfStrings array containing many strings of an undetermined length
     * @return A list of Strings where each string must have more than 1 vowel.
     *
     */
    public List<String> removeWordsWithOnlyOneVowel(List<String> listOfStrings) {
        return new ArrayList<>();
    }

}
