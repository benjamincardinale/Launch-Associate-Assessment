package assessment;

import java.util.ArrayList;
import java.util.List;

public class ReverseList {

    /**
     * Complete the method reverseList
     * Return the list with its contents in reverse order.
     * @param listOfStrings This method takes in a list of string of undetermined length
     * @return The list with its contents in reverse order.
     * Meaning the last item in the original list will be the first item in your result list and so on.
     * EX: {"a", "b", "c"} -> {"c", "b", "a"}
     */

    public List<String> reverseList(List<String> listOfStrings) {

        return listOfStrings;
    }
}
